/**
 * FullCalendar v6 injects base + list CSS at module load via injectStyles().
 * Import this wherever markup uses fc-list-* classes without mounting FullCalendar.
 */
import "@fullcalendar/list";
